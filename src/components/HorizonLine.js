import React from "react";

const HorizonLine = ({ text }) => {
  return (
    <div
      style={{
        width: "300px",
        textAlign: "center",
        borderBottom: "3px dashed #fff",
        lineHeight: "0.1em",
        margin: "10px 0 40px",
      }}
    >
    </div>
  );
};

export default HorizonLine;